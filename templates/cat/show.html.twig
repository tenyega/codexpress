{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category.title }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ category.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_index') }}">back to list</a>

    <a href="{{ path('app_cat_edit', {'id': category.id}) }}">edit</a>

    {{ include('cat/_delete_form.html.twig') }}
{% endblock %}
